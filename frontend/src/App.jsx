import{useState}from"react";import{Github,GitBranch,Code2,Bot,Search,ArrowRight,LoaderCircle,AlertCircle}from"lucide-react";import{analyzeRepository,getFile,askQuestion}from"./api";import Graph from"./components/Graph";import Chat from"./components/Chat";
export default function App(){const[url,setUrl]=useState(""),[repo,setRepo]=useState(null),[file,setFile]=useState(""),[code,setCode]=useState(""),[busy,setBusy]=useState(false),[err,setErr]=useState("");
async function analyze(e){e.preventDefault();setErr("");setRepo(null);try{setBusy(true);setRepo(await analyzeRepository(url))}catch(x){setErr(x.response?.data?.detail||x.message)}finally{setBusy(false)}}
async function openFile(p){try{setFile(p);setCode((await getFile(repo.repo_id,p)).content)}catch(x){setErr(x.response?.data?.detail||"Unable to load file.")}}
return <div className="app"><header><div className="brand"><GitBranch/> <b>GitInsight</b><small>Repository Intelligence</small></div><a href="https://github.com" target="_blank"> <Github/> GitHub</a></header>
<main><section className="hero"><span className="badge">AI-powered codebase understanding</span><h1>Understand any GitHub repository.</h1><p>Analyze project structure, explore dependencies, inspect source code, and ask questions about your codebase.</p><form onSubmit={analyze}><div className="input"><Github/><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://github.com/owner/repository"/></div><button disabled={busy}>{busy?<LoaderCircle className="spin"/>:<Search/>}{busy?"Analyzing":"Analyze"}{!busy&&<ArrowRight/>}</button></form>{err&&<div className="error"><AlertCircle/>{err}</div>}</section>
{repo?<section className="workspace"><div className="repohead"><div><small>ANALYZED REPOSITORY</small><h2>{repo.repo_url}</h2></div><div className="stats"><b>{repo.stats.files}<small>Files</small></b><b>{repo.stats.dependencies}<small>Dependencies</small></b></div></div>
<div className="grid"><aside className="panel files"><h3><Code2/> Source Files</h3>{repo.files.map(f=><button className={file===f.path?"active":""} onClick={()=>openFile(f.path)} key={f.path}>{f.path}</button>)}</aside><div><div className="panel"><h3><GitBranch/> Dependency Graph</h3><Graph nodes={repo.nodes} edges={repo.edges}/></div>{file&&<div className="panel code"><h3><Code2/> {file}</h3><pre>{code}</pre></div>}</div><Chat repoId={repo.repo_id} ask={askQuestion}/></div></section>:<div className="features"><Feature icon={<GitBranch/>} title="Repository Map" text="Explore relationships between files and modules."/><Feature icon={<Code2/>} title="Code Explorer" text="Inspect source files from the analyzed repository."/><Feature icon={<Bot/>} title="AI Assistant" text="Ask natural-language questions about the codebase."/></div>}</main><footer>GitInsight • AI-powered repository intelligence</footer></div>}
function Feature({icon,title,text}){return <div className="feature">{icon}<h3>{title}</h3><p>{text}</p></div>}